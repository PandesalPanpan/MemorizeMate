services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "${DEV_PORT:-5174}:5174"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env
